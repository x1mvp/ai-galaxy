[tool.pytest.ini_options]
testpaths     = ["tests", "tests/integration"]
asyncio_mode  = "auto"
markers = [
    "db: tests that require a live PostgreSQL instance (deselect with '-m not db')",
]
addopts = """
    -v
    --tb=short
    -m "not db"
"""
